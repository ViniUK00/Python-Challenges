#Your function should return True if the string is a palindrome(A string is a palindrome when it is the same when read backwards), and False otherwise.

def palindrome(string):
    if string == string[::-1]:
        return True
    else:
        return False

